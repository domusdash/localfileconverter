name: blueprintconverter
region: nyc
domains:
- domain: localfileconverter.app
  type: PRIMARY
ingress:
  rules:
  - component:
      name: blueprintconverter
    match:
      path:
        prefix: /
static_sites:
- github:
    branch: main
    deploy_on_push: true
    repo: domusdash/blueprintconverter
  name: blueprintconverter
  output_dir: .
